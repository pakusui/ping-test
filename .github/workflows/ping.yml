name: Ping my site

on:
  schedule:
    - cron: "*/30 * * * *"  # 30分ごとに実行（UTC基準）
  workflow_dispatch:        # 手動実行も可能

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Access the site
        run: curl -s https://web-chat-gpt-mkbf.onrender.com/
